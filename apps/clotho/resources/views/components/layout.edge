<!DOCTYPE html>
<html lang="en-us">
  <head>
    <meta charset="utf-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1" />
    @vite(['resources/js/app.js', 'resources/css/app.css'])
    @if ($slots.meta)
      {{{ await $slots.meta() }}}
    @endif
  </head>
  <body>
    <main class="flex h-dvh flex-col bg-gray-800 text-gray-100">
      {{{ await $slots.main() }}}
    </main>
  </body>
</html>
